# server/auth

Better Auth configuration: Google sign-in and the organization plugin (organizations are our workspaces). A `getAuth()` factory builds this over a request-scoped `getDb()`, added in Stage 4.
